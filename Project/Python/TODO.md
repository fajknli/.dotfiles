# TODO

## 短期

- [ ] 实现 `/clear-memories before <id>` 命令，选择性清理早期低质量记忆
- [ ] 添加 `jieba` 中文分词支持，改善自然语言搜索效果
- [ ] 检索完全匹配加成（`exact_match_bonus`），完整匹配时提权 1.5 倍
- [ ] 提高关键词加成权重至最高 2 倍（当前 1.5 倍）

## 中期

- [ ] 候选集 SQL 粗筛：记忆数量超过 500 时，先用 `LIKE` 过滤再向量检索
- [ ] 查询改写：用小模型将口语化查询规范化（"那个数据库" → "PostgreSQL"）
- [ ] `/stats` 增加记忆质量评分，标识低质量/碎片化记忆
- [ ] 记忆自动合并：识别重复或高度相似的记忆，提示用户合并

## 长期（v0.6.0+）

- [ ] **MemoryManager**：自动分类新记忆到 Wing/Room，找不到进杂项
- [ ] **矛盾检测**：向量相似度 > 0.85 且含否定词时写入 `contradicts` 关系
- [ ] **观点版本管理**：`superseded_by` 字段标记被替代的旧记忆
- [ ] **关系提取升级**：支持 `supports`、`extends` 关系类型
- [ ] **夜间巡逻**：低频扫描图谱，清理低置信度关系，标记孤立记忆
- [ ] **图游走检索**：沿关系边探索历史决策、矛盾、类似情境
- [ ] **LoRA 微调**：积累对话数据后微调主模型，形成数据飞轮

## 技术债务

- [ ] `retrieval.py` 评分循环中的向量计算可批量优化
- [ ] `storage.py` `_init_db` 方法过长，考虑拆分为多个迁移脚本
- [ ] 添加单元测试覆盖核心检索逻辑
- [ ] CLI 错误信息统一用 `rich` 风格输出

## 想法/实验

- [ ] 支持多模态记忆（图片 OCR、音频转录）
- [ ] 记忆导出格式支持 Markdown 便于阅读
- [ ] Web UI 替代 CLI 对话界面
- [ ] 插件系统：允许用户自定义 Distiller prompt 模板

下一步你可以做
清理调试信息（把 [DEBUG] 打印删掉或改成 logging.debug）

提交代码

bash
git add .
git commit -m "feat: AI 主动矛盾提示 + 通用规则触发"
继续优化：

缓存矛盾对（避免每次检测都 O(n²)）

支持按话题检测矛盾（/reflect --topic "数据库"）

矛盾网络可视化

写文档（更新 README，告诉别人这个项目能做什么）

休息（你已经做了很多）

nmap -sC -sV 192.168.1.1 版本探测和脚本扫描
nmap -O 192.168.1.1 操作系统探测
nmap -sn 192.168.1.0/24 快速发现主机
nmap 192.168.1.1 扫描最常用1000个tcp端口
nmap -p- 192.168.1.1    扫描所有65535个端口
nmap -p 22,80,443,3306 192.168.1.1  # 扫描指定端口
nmap -p 1-1000 192.168.1.1           # 扫描端口范围
nmap -p http,https,ssh 192.168.1.1   # 扫描服务名（在/etc/services中定义)
nmap -sU -top-ports 100 192.168.1.1  UDP扫描
    UDP扫描很慢（因为协议是无连接的），所以通常用 --top-ports N 来只扫描最常见的N个UDP端口（如DNS, SNMP, DHCP等）。
nmap -sV 192.168.1.1    端口服务版本探测
nmap -A 192.168.1.1 全部扫描(最慢)
nmap -T4 192.168.1.1 调整扫描速度
    -T 选项（0-5）控制扫描速度/激进程度。-T3 是默认，-T4 更快更常用，-T5 极速（可能丢包或不准确）。

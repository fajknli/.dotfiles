:'a,.! wc -l    设置标记a并计数标记a到当前行的行数量
"Ay,"Ap 追加器复制和粘贴(直接按下这3个字符，不用在命令里输入)
"ay,"ap 覆写复制和粘贴(直接按下这3个字符，不用在命令里输入)
:reg a  查看寄存器内容
:let @a = '' 清空寄存器
qq0"Ay$jq   录制复制当前行到寄存器a并下移(@q 执行)
:set paste 进入粘贴模式(临时粘贴内容用)
:set nopaste 关闭粘贴模式


#!/bin/sh

# 检查网络连接
#for i in {1..3}; do
if ping -q -c 1 -W 1 163.com > /dev/null; then

    echo "网络连接成功，执行时间同步..."

  sudo ntpdate cn.pool.ntp.org

else

  echo "网络连接失败，无法同步时间"

fi

#sleep 60

#done

#exit 0

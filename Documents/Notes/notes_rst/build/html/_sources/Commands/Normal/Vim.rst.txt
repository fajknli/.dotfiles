Vim
#####

1. 移动
=============

1.1 光标移动
------------

hjkl ==> 左下上右

1.2 页移动
-------------

:Ctrl + F: 向前滚动一屏
:Ctrl + B: 向后滚动一屏
:Ctrl + D: 向前滚动半屏
:Ctrl + U: 向前滚动半屏
:H: 将光标置于屏幕顶部(High)
:M: 将光标置于屏幕中部(Middle)
:L: 将光标置于屏幕底部(Low)
:gg: 将光标置于文件开头
:G: 将光标置于文件末尾
:n: 将光标置于文件第n行
:Ctrl + e: 文件向下滚动一行
:Ctrl + y: 文件向上滚动一行
:zt: 将当前行移动到屏幕顶部
:zz: 将当前行移动到屏幕中间
:zb: 将当前行移动到屏幕底部

1.3 词移动
-------------

:w: 光标移动到下个单词的开头("w" 命令将把你带到下一行的第一个单词,这比使用 "l" 要快得多。)
:W: 光标移动到下一段词的开头(就是包括标点符号，相当于以空格分隔的每一段词，下面的"B"同理)
    
:b: 光标移动到前一个单词的开头("b" 则在反方向完成这个功能)
:B: 光标移动到上一段词的开头(包括这段词的标点符号)
    
:e: 移动到当前单词和下一个单词的末尾
:E: 移动到下一段词的末尾

:ge: 移动到上一个单词的末尾
:gE: 移动到上一个段词的末尾

1.4 行移动/查找并跳转
-----------------------

:gg/G : "gg"移动到文件开头,"G"移动到文件末尾(末行第一个字符)

:f/F: 移动光标到查找的词上
:t/T: 移动光标到查找的词的前一个地方上

:;/,: ";"可以重复操作，用在以上的条件下,也就是重复查找,","反向重复

1.5 括号匹配
-------------

:%: 百分号，可以在括号上跳转到与其匹配的另外的括号上，如果不在括号上使用，则会正向匹配临近的一个括号上

1.6 在文本中查找一个单词
--------------------------

:/: "/"查找词，/\<hi\>,查找整个词，"\<"是匹配开头对应的词，"\>"就是匹配末尾匹配的词
:/^: "/^"查找此行的第一个匹配的词
:/xxx$: "/xxx$"查找此行的最后一个匹配的词, xxx
:/he.lo: ".",匹配任何单个字符
:/hello\.: "\",如果你确实想匹配点字符，可以在前面加一个反斜杠去消除它的特殊含义。

1.7 标记来回跳转
----------------

当你用 "G" 命令跳到另一个地方，Vim 会记住你从什么地方跳过去的。这个位置成为一
个标记，要回到原来的地方，使用如下命令:

::

    `` / ''

` 是反引号，用单引号 ' 也可以。
如果再次执行这个命令你会跳回去原来的地方，这是因为 "`" 命令本身是个跳转，
它记住了自己跳转前的位置。

1.8 书签设置和跳转
-------------------

命令 "ma" 用 a 标记当前的光标位置。你可以
在文本中使用 26 个标记 (a 到 z)。这些标记是不可见的要跳到一个你定义的标记，可以使用命令\`{mark}，这里 {mark} 是指定义标记的那
个字母。所以，移到标记 a 的命令是:

::

    `a 跳转到标记处
    'a 跳转到标记处的第一个字符

如下命令取得所有的标记的列表:

::

    :marks


.. warning::

    不包括 "fx" 和 "tx" 这些行内查找命令或者 "w" 和 "e" 等词移动命令
    另外 "j" 和 "k" 不会被当做是一次 "跳转"，即使你在前面加上计数前缀使之移动
    到很远的地方也不例外

2. 修改
===========

:cc: 删除此行内容并进入插入模式
:ciw: 修改光标所在的词
:c2w: 删除之后的两个词并进入插入模式
:x: 表示 dl (删除当前光标下的字符)
:X: 表示 dh (删除光标左边的字符)
:D: 表示 d$ (删除到行尾)
:C: 表示 c$ (修改到行尾)
:s: 表示 cl (修改一个字符
:S: 表示 cc (修改一整行)
:xp: 交换两个相邻的字符
:cis: 修改这句话     
:r: 只替换光标处的一个词
:R: 从光标处开始向后进行替换，退格键进行删除替换，找回原来

-----

:x:      删除光标下的字符 ("dl" 的缩写)
:X:      删除光标前的字符 ("dh" 的缩写)
:D:      从当前位置删除到行尾: ("d$" 的缩写)
:dw:     从当前位置删除到下一个单词开头
:db:     从当前位置删除到前一个单词的开头
:diw:    删除光标上的单词 (不包括空白字符)
:daw:    删除光标上的单词 (包括空白字符)
:dG:     删除到文件末
:dgg:    删除到文件首
:~:      修改光标下字符的大小写，并移动到下一个字符。这不是一个操作符(除非设置了 'tildeop')，所以你不能连接一个动作命令。这个命令在可视模式下也有效，它会改变被选中的所有文本的大小写
:I:      移到当前行的第一个非空字符并启动插入模式
:A:      移动到行尾并启动插入模式

如果你用 "c" 代替 "d"，这会变成修改命令；而改用 "y"，则变成拷贝命令，等等.










.. note::

    "cis" 包括 "c" (change，修改) 操作符和 "is" 文本对象。这表示 "Inner Sentence"
    (内含句子)。还有一个文本对象是 "as" ("A Sentence")，区别是 "as" 包括句子后面的
    空白字符而 "is" 不包括。如果你要删除一个句子，而且你还想同时删除句子后面空白字
    符，就用 "das"；如果你想保留空白字符而替换一个句子，则使用 "cis"

    v模式,可视模式下也可以使用as,is这样的

1. 重复操作
--------------

"." 是 Vim 中一个非常简单而有用的命令。它重复最后一次的修改操作

"." 命令重复任何除 "u" (撤销)，CTRL‑R (重做) 和冒号命令外的修改





3. 记录与回放命令(宏)
-----------------------

1. "q{register}" 命令启动一次击键记录，结果保存到 {register} 指定的寄存器中。寄存器名可以用 a 到 z 中任一个字母表示。 

2. 输入你的命令。

3. 键入 q (后面不用跟任何字符) 命令结束记录。

4. 现在，你可以用 "@{register}" 命令执行这个宏。

"@a" 命令可以通过计数前缀修饰，使操作重复指定的次数。

.. note::

    你可能有多个地方需要修改。只要把光标移动到相应的位置并输入 "@a" 命令即可。如果
    你已经执行过一次，你可以用 "@@" 完成这个操作，这更容易输入一些。例如，你上次使
    用 "@b" 命令引用了寄存器 b，下一个 "@@" 命令将使用寄存器 b。
    如果你对回放命令和 "." 命令作一个比较，你会发现几个区别。首先，"." 只能重
    复一次改动。而在上例中，"@a" 可以重复多次改动，还能够执行移动操作。第二，"."
    只能记住最后一次变更操作。而寄存器执行命令允许你记录任何操作并使用像 "@a" 这样
    的命令回放这些被记录的操作。最后，你可以使用 26 个寄存器，因此，你可以记录多达
    26 个命令序列。

3.1 寄存器
------------

假设你在寄存器 a 中记录了一些命令。当你通过 "@a" 执行这些命令时，你发现这
些命令有些问题。这时你可以重新录一次，但这样你可能还会犯其它错误。其实，你可以
使用如下窍门:

::

    "ap     将 a 寄存器中的文本,粘贴出来

    0"ay$   修改好后，回到行首复制从行首到末尾的内容

    dd      删除这个临时行

追加寄存器内容

要附加内容到一个寄存器中，可以使用大写的寄存器名

这种方法在宏记录，拷贝和删除命令中都有效。例如，你需要把选择一些行到一个寄存器
中，可以先这样拷贝第一行:

::

    "aY     将此行复制到a寄存器,如果每次都对不同行使用这个，那么只会替换掉原来的内容，而不是追加
    
    "AY     将此行追加到a寄存器,放在其他内容的后面
    

5. 分割窗口
=============

1. 打开一个新窗口:
--------------------

::

    :split

CTRL‑W w 命令可以用于在窗口间跳转

**实际上，按住CTRL‑W,hjkl可以随便移动到想去的那个窗口**

2. 关 闭 窗 口
---------------

::

    :close

退出也可以退出那个窗口，只是最后一个窗口也可能会退出，:close不会退出最后一个窗口


3. 关 闭 所 有 其 它 窗 口
---------------------------------

关闭其他窗口，只留当前这一个

::

    :only

4. 用另一个文件分割窗口
--------------------------------------------

::

    :split two.c

    or

    :new ....

一样的

5. 窗口大小
------------------------------------

扩大窗口:

::

    CTRL‑W +

缩小窗口:

::

    CTRL‑W ‑

"4 CTRL‑W +" 会使窗口增高4 行

------

要把一个窗口设置为指定的高度，可以用这个命令:

::

    {height}CTRL‑W _

就是先输入一个数值，然后输入 CTRL‑W 和一个下划线 (在美式英语键盘中就是 Shift
加上 "‑")。

6. 垂直分割
--------------

::

    :vsplit ...

    or

    :vnew ...

    :vertical ...

7. 移动窗口
---------------------------------

::

    CTRL‑W H    大写的H
    CTRL‑W J
    CTRL‑W K
    CTRL‑W L

    hjkl 你懂的

8. 对所有窗口执行命令
--------------------------------------------

::

    :qall   quit all 关闭每一个窗口

    :wall   write all 全部保存

    :wqall  ":qall" 和 "wall" 的组合命令

    :qall!  退出 Vim 并放弃所有修改

9. 启动vim 时，多窗口文件启动
--------------------------------------------

要让 Vim 为每个文件打开一个窗口，可以使用 "‑o" 参数:

::
    
    vim ‑o one.txt two.txt three.txt    水平多窗口

    "‑O" 参数用于垂直分割窗口。

    vim ‑o one.txt two.txt three.txt    垂直多窗口

10. 用 vimdiff 显示文件差异
--------------------------------------------

在终端中输入如下命令: (不是在 Vim 中)

::

    vimdiff main.c~ main.c

6. 查找
============



3. 查找一个单词在一些文件里
----------------------------

举个例子，假设想编辑所有包括单词 "frame_counter" 的 C 源文件，你可以执行如
下命令:

::

    vim `grep ‑l frame_counter *.c`     

整个命令用反引号 (`) 包起来，这告诉 UNIX 的外壳使用该命令的输出作为命令行
的一部分。于是，grep 命令产生一个文件列表，并作为 Vim 的命令参数。Vim 将编辑
grep 列出来的所有文件。你可以通过 ":next" 和 ":first" 命令一个一个处理这些文
件。

上面的命令只是找到包括单词的那个文件。你还需要知道单词在该文件中出现的地方。
Vim 有一个内置的命令用于在一组文件中找一个指定的字符串。例如，如果你想在所
有的 C 文件中查找 "error_string"，可以使用如下命令:

::

    :grep error_string *.c

    这会使 Vim 在所有指定的文件 (*.c) 中查找 "error_string"。Vim 会打开第一个匹配
    的文件并将光标定位在第一个匹配行。要到下一个匹配行 (无论在哪个文件)，可以执行
    "cnext" 命令。要回到上一个匹配行，可以用 ":cprev" 命令。使用 "clist" 可以看到
    所有的匹配点。
    ":grep" 命令会使用一个外部的程序。可能是 grep (在 Unix 上) 或者 findstr
    (在 Windows 上)。你可以通过 'grepprg' 选项修改这个设置。


7. 命令
===============

1. global
---------------------------------

":global" 命令是 Vim 中一个更强大的命令 (之一)。它允许你找到一个匹配点并且在那
里执行一个命令。它的一般形式是:

::

    :[range]global/{pattern}/{command}

    这有点像 ":substitute" 命令。只是它不替换文本，而是执行 {command} 指定的命令。

.. note::

    global 中执行的命令只能是冒号命令。普通模式命令不能在这里使用。如果需
    要，可以使用 :normal 命令

假设你要把 "foobar" 修改为 "barfoo"，但只需要修改 C++ 风格的注释中的内容。这种
注释以 "//" 开头。所以可以使用如下命令:

::

    :g+//+s/foobar/barfoo/g

    其中+号可以替换成,;-
    不能替换成\

    :g      代表:global
    +       代表/
    //      代表着匹配着"//"
    s       代表着:s命令，也就是之前的替换操作

    i全局命令的默认范围是整个文件，所以这个例子中没有指定范围。这一点与
    ":substitute" 是不同的。后者只作用于一行。
    这个命令并非完美。因为 "//" 可能出现在一行的中间，但替换命令会把前后的匹配
    点都替换了。

    像 ":substitute" 一样，这里也可以使用各种各样的匹配模式。当你从后面的章节中学
    会更多的关于模式的知识，它们都可以用在这里。

1. 反转行顺序(文章内容上下颠倒)
''''''''''''''''''''''''''''''''''''''''''''

:global 命令可以和 :move 行反转了次序。命令是:

::

    :global/^/move 0

    缩写：

    :g/^/m 0

    正则表达式 "^" 匹配行首 (即使该行是一个空行)

    :move 命令将匹配的行移动到那个想像中的第 0 行之后。这样匹配的行就成了文件中的第一行。由于 :global 命令不会
    被改变了的行号搞混，该命令继续匹配文件中剩余的行并将它们一一变为首行。

i这对一个行范围同样有效。先移动到第一行上方并做标记 't' (mt)。然后移动到范围的
最后一行并键入:

::

    :'t+1,.g/^/m 't


2. read 
------------------------

1. read 读取文件
''''''''''''''''''''''''''''''''''

读取文件并将其内容插入：

::

    :[range]read {file name}

    其中[range]无论是多少，都取最后一值，比如0,3 这就会从第3行下面开始插入。所以0就可以在这里被使用，也就是在文件开头插入

    :$r ...     在文件末尾插入
    :0r ...     在文件开头插入
    :.r ...     在光标所在行插入

2. 保存部分行
''''''''''''''

":write" 命令。在没有指定范围的时候它写入全文，而指定范围的时候它只写入范围指定的行:

::

    :.,$write {file name}       这个命令将当前光标所在行到文件末尾的内容保存到指定文件内

3 添加当前文件内容到之外文件的内容
'''''''''''''''''''''''''''''''''''''

::

    :.write {file in same CWD}     把当前行写入覆盖到{file}下，要是同一个CWD的文件

    :.write >>{file in same CWD}   把当前行追加写入到{file}下，要是同一个CWD下的文件


4. 排版文本
'''''''''''''''

先设置个自动换行:

::

    :set textwidth=72

Vim 排版当前的段落:

::

    gqap    排版光标所在段落或者光标以下最近的段落

    gq      表示编排段落
    ap      表示一个段落,光标所在段落或者光标以下最近的段落

这个命令用 "gq" 开始，作为操作符，然后跟着 "ap"，作为文本对象，该对象表示 "一 段" (a paragraph)。"一段" 与下一段的分割符是一个空行。


.. note::

    只包括空白字符的空白行不能分割 "一段"。这很不容易分辨。

除了用 "ap"，你还可以使用其它 "动作" 或者 "文本对象"。如果你的段落分割正确，你
可以用下面命令排版整个文档:

::

    gggqG

    "gg" 跳转到第一行，"gq" 是排版操作符，而 "G" 是跳转到文尾的 "动作" 命令。


5. 改变大小写
''''''''''''''

"gU" 操作符,改变到大写状态

"gu" 操作符,改变到小写状态

以上操作符号对一个单词执行后，要加个移动操作，才能显示出来，例如："gUl","gUj",因为"gu/U"操作符还未完整，一般都是"gUw",整词大写

.. note::

    你还可以用 "g~" 来交换大小写。所有这些命令都是操作符，所以它们可以用于 "动作"
    命令，文本对象和可视模式。
    要让一个操作符作用于当前行，可以执行这个操作符两次。例如，"d" 是删除操作
    符，所以删除一行就是 "dd"。相似地，"gugu" 使整一行变成小写。这可以缩成 "guu"。
    "gUgU" 可以缩成 "gUU" 而 "g~g~" 则是 "g~~"。例如:

3. ! 调用外部命令
-------------------------------

1.对文本执行命令
'''''''''''''''''

"!"用一个外部程序对一个文本块进行过滤:

::

    :[range]!{program}

"!!" 命令用于对当前行执行过滤命令。在 Unix 上，"date" 命令能打印当前的时间和日
期，所以，"!!date<Enter>" 用 "date" 的输出代替当前行。这在为文件加入时间戳的时
候非常有用。

直接在普通模式下输入"!!"就会出来":.!",然后加上date命令，执行后就可以输出了,这将覆盖掉原来这行的内容

::

    :.!date

    这将覆盖掉原来这行的内容

2. 读取一个命令的输出
'''''''''''''''''''''''

读取并粘贴当前所在目录下的文件

::

    :r !ls

    :r      :read 命令
    !ls     看上面那个内容,执行ls命令，"!"号就是在vim里的执行ls命令的。。。

这些命令还可以带参数。而且前面还可以带一个范围用于告诉 Vim 把这行放在什么 地方:

::

    :0r !date -u

    这将用 UTC 格式把当前的时间插入到文件开头。

3. 把文本用于命令的输出
''''''''''''''''''''''''

Unix 命令 "wc" 用于统计单词数目。要统计当前文件有多少个单词，可以这样:

::

    :w !wc

    :w      :write,:0w,:$w,:nw 单行内容单词统计
    !wc     执行对全文本执行wc命令

    1   6  29

    "wc" 命令惜字如金。这表示你有 1 行，6 个单词和 29 个字符。

.. warning::

    不要错写成:

    :write! wc
    
    这会强制把当前文件存到当前目录的 "wc" 文件中。在这里空格的位置是非常重要的！


Mon Jan 29 17:20:22 CST 2024


4. :substitute命令 替换
----------------------------------------------------

1. :substitute命令/:s
''''''''''''''''''''''

":substitute" 命令使你可以在连续的行中执行字符串替换。下面是这个命令的一般形
式:

::

    :[range]substitute/from/to/[flags]

    其中

    :substitute     命令太长，缩小为":s"
    [range]         "%" 表示命令作用于全部行。如果不指定行范围，":s" 命令只作用在当前行上
    /from/to/       把[range]指定范围内的"from"替换为"to"
    [flages]        "g" (global，全局) 标记,没有全局标记，只会匹配范围内行内第一个可匹配值
                    "p" (print，打印)，用于在命令执行的时候打印出最后一个被修改的行
                    "c" (confirm，确认) 标记会在每次替换前向你询问是否需要替换

                        y          Yes，是；执行替换
                        n          No，否；跳过
                        a          Quit，退出；不再执行任何替换
                        q          Last，最后；替换完当前匹配点后退出
                        l          向上滚动一行
                        CTRL‑E     向下滚动一行
                        CTRL‑Y     All，全部；对剩下的匹配点全部执行替换，不需要再确认


2. 范围替换可使用匹配模式
''''''''''''''''''''''''''

":s" 命令中的 "from" 部分实际上是一个 "匹配模式"
要替换行首的 "the" 可以这样写:

::

    :s/^the/these/

3. 匹配中使用正斜杠
''''''''''''''''''''

如果你要在 "from" 或者 "to" 中使用正斜杠，你需要在前面加上一个反斜杠。例如:

::

    fr/om t/o

    :%s/fr\/om/t\/o/

    执行上面的命令后，前面的fr/om t/o 变成了以下内容

    t/o t/o

1 单词替换
^^^^^^^^^^^^^^^^

替换命令可以在全文中用一个单词替换另一个单词:

::

    :%s/four/4/g

"%" 范围前缀表示在所有行中执行替换。最后的 "g" 标记表示替换行中的所有匹配点。
如果你有一个像 "thirtyfour" 这样的单词，上面的命令会出错。这种情况下，这
个单词会被替换成 "thirty4"。要解决这个问题，用 "\<" 来指定匹配单词开头:

::

    :%s/\<four/4/g

显然，这样在处理 "fourteen" 的时候还是会出错。用 "\>" 来解决这个问题:

::

    :%s/\<four\>/4/g

如果你在编码，你可能只想替换注释中的 "four"，而保留代码中的。由于这很难指定，
可以在替换命令中加一个 "c" 标记，这样，Vim 会在每次替换前提示你:

::

    :%s/\<four\>/4/gc

2. 多文件里词替换
^^^^^^^^^^^^^^^^^^^

假设你需要替换多个文件中的单词。你的一个选择是打开每一个文件并手工修改。但是 使用记录和回放命令会快很多。 假设你有一个包括有 C++ 文件的目录，所有的文件都以 ".cpp" 结尾。有一个叫 "GetResp" 的函数，你需要把它改名为 "GetAnswer"。

::

    vim *.cpp                       启动 Vim，用当前目录的所有 C++ 文件作为文件参
                                    数。启动后你会停在第一个文件上。
    qq                              用 q 作为寄存器启动一次记录。
    :%s/\<GetResp\>/GetAnswer/g     在第一个文件中执行替换。
    :wnext                          保存文件并移到下一个文件。
    q                               中止记录。
    @q                              回放 q 中的记录。这会执行又一次替换和":wnext"。你现在可以检查一下记录有没有错。
    999@q                           对剩下的文件执行 q 中的命令

Vim 会在最后一个文件上报错，因为 ":wnext" 无法移到下一个文件上。这时所有的文件
中的操作都完成了。

.. warning::

    在回放记录的时候，任何错误都会中止回放的过程。所以，要 注意 保证记录中
    的命令不会产生错误。

这里有一个陷阱: 如果有一个文件不包含 "GetResp"，Vim 会报错，而整个过程会中止，
要避免这个问题，可以在替换命令后面加一个标记:

::

    :%s/\<GetResp\>/GetAnswer/ge

    "e" 标记通知 ":substitute" 命令找不到不是错误。
    
3. 把 "Last, First" 改成 "First Last"
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

::

    :%s/\([^,]*\), \(.*\)/\2 \1/

我们把这个命令分解成几个部分。首先，很明显它是一个替换命令。"%" 是行范围，表示
作用于全文。这样替换命令会作用于全文的每一行。
替换命令的参数格式是 "from/to"，正斜杠区分 "from" 模式和 "to" 字符串。所
以，"from" 部分是:

::

    \([^,]*\), \(.*\)

    第一对 \( 和 \) 之间的部分匹配 "Last"           \(  \)
    匹配除逗号外的任何东西                          [^,]
    任意多次                                        *
    匹配逗号                                        ,
    第二对 \( 和 \) 之间的部分匹配 "First"          \(  \)
    匹配任意字符                                    .
    任意多次                                        *

在 "to" 部分，我们有 "\2" 和 "\1"。这些称为 "反向引用"。它们指向前面模式中的
\( 和 \) 间的部分。"\2" 指向模式中的第二对 \( 和 \) 间的部分，也就是 "First"
名 (译者注: 英文中 Last Name 表示姓，即家族名，后面的 First Name 表示名字)。
"\1" 指向第一对 \( \)，即 "Last" 名。

你可以在替换部分使用多达 9 个反向引用。"\0" 表示整个匹配部分。还有一些特殊
的项可以用在替换命令中。


4. 命令范围
''''''''''''''

接着上面的例子，最简单的范围表达形式是 "{number},{number}"。例如:

::

    :1,5s/this/that/g

    其中"1,5" 就是[range],表示第一行到第五行的内容里 

如果只用一个数值，表示某个指定的行:

::

    :54s/President/Fool/

有些命令在不指定范围的时候作用于整个文件。要让它只作用于当前行可以用当前行范围
标识 "."。":write" 命令就是这样: 不指定范围的时候，它写入整个文件，如果要仅写
入当前行，可以这样:

::

    :.write otherfile

文件的第一行行号总是 1，最后一行又是多少呢？"$" 字符用于解决这个问题。例如，要
修改当前行到文件末的全部内容，可以这样:

::

    :.,$s/yes/no/

    我们前面使用的 "%" 就是 "1,$" 的缩写形式，表示从文件首到文件末。

1. 使用匹配模式
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

假设你正在编辑一本书中的一章，并且想把所有的 "grey" 修改成 "gray"。但你只想修
改这一章，不想影响其它的章节。另外，你知道每章的开头的标志是行首的单词为
"Chapter"。下面的命令会对你有帮助:

::

    :?^Chapter?,/^Chapter/s=grey=gray=g

    ?....?      表示向后匹配，默认什么都不加就是向前匹配
    同样，"/^Chapter/" 用于向后查找下一章
    =           等于号表示"/"符号，就是代表那个意思，不行的话还是用"/"吧

2. 匹配偏移，使用+/-号
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

上面的方案其实还是有问题的: 如果下一章的标题行中包括 "grey"，这个 "grey" 也会
被替换掉。如果你正好想这样就最好，可是正好你不想呢？这个时候你需要指定一个偏
移。
要查找一个模式，并且使用它的后一行，需要这样:

::

    /Chapter/‑1

你可以用任意数值代替命令中的 1。要定位匹配点下的第二行，要这样:

::

    /Chapter/+2

偏移还可以用于其它范围指定符。看一下下面这个例子:

::

    :.+3,$‑5

    1+1,$-4s/Blook/uuuuuuuuuuuuuuuuuu/

    这种偏移包含了最终偏移点内容,也就是说，1+1行的内容如果匹配，也会被修改

这指定当前行下面第三行到文件末倒数第五行的范围。

3. 使用标记作为命令范围
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

例如，用 "mt" 标记开头，再用"mb" 标记结尾。然后你就可以用标记表示一个范围 (包括标记的那一行):

::

    :'t,'b

4. 可视模式下作为范围
^^^^^^^^^^^^^^^^^^^^^^^^

可以在可视模式中选中一些行。如果你现在输入 ":" 启动冒号命令模式，你会看到:

::

    :'<,'>

    现在，你可以输入剩下的命令，这个命令的作用范围就是可视模式中指定的范围。

.. note::

    如果使用可视模式选中行的一部分，或者用 CTRL‑V 选中一个文本列块，然后执
    行冒号命令，命令仍作用于整行，而不只是选中的范围。这可能会在以后的版本
    中修正

    v9.1未修复

'< 和 '> 实际上是标记，分别标识可视模式的开始和结尾,你还可以用标记跳转命令 "'<" 跳转到选中文本的开始处

还可以把这个标记和其它标记混合，例如:

::

    :'>,$

    这表示从选中部分的结尾到文件末

5. 预先指定匹配行数
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

i如果你知道要修改多少行，你可以先输入一个数值再输入冒号。例如，如果你输入
"5:"，你会得到:

::

    :.,.+4

    其中从本行开始向下4行，一共是5行


8. 选择
========

1. 可视列块模式
----------------

CTRL‑V 命令可以选中一个矩形文本块。有几个命令是专门用来处理这个文本块的。

当最后一个移动命令是 "$" 时，整个可视列块
将被扩展到每一行的行尾。这种状态在你使用垂直移动命令的时候一直被保持，直到你使
用水平移动命令为止,就是说，用 "j" 命令会保持这种状态，而 "h" 会退出。

这个有时候挺方便的，一直hjkl总是担心选区无法包含所有

1. 插入文本
'''''''''''''''''''''''''''''''''

"I{string}<Esc>" 命令把 {string} 插到可视列块的每一行的左边。你用 CTRL‑V 进入
可视列块模式，然后移动光标定义一个列块。接着输入 I 进入插入模式，并随后输入文
本。这时，你输入的内容只出现在第一行。
然后你输入 <Esc> 结束输入，刚才输入的字符串将神奇地出现在每一行的可视区的
左边。

如果选中的块经过一个短行，并且这行没有任何内容包括在可视列块中，则新的文本不会
被插入到该行中

----

"A" 命令的效果与 "I" 命令一样，只是把文字插入可视列块的右边，而且在短行中会插
入文字。这样，你有在短行中插入文字与否的不同选择。

"A" 在如下情况会有一些特别: 选中一个可视列块然后用 "$" 命令使可视列块扩展
到行尾。然后用 "A" 命令插入文本，文件将被插入到 "每一行" 的行尾。

挺有用的

2. 修改文本
'''''''''''''''''''''''''''''''''

可视列块中的 "c" 命令会删除整个可视列块并转入 "插入" 模式,然后输入内容，在<Esc>退出，就会给这可视化块里填充刚刚的内容
与 "I" 命令一样，短行不会发生变化。而且在插入的过程中，你不能断行

在可视化块下"C" 命令从块的左边界开始删除所有行的后半段，然后状态切换到 "插入" 模式让你输
入文本。新的文本被插入到每一行的末尾

调整可视化块里的字符的大小写

:~:   交换大小写  (a ‑> A 而 A ‑> a)
:U:   转换成大写  (a ‑> A 而 A ‑> A)
:u:   转换成小写  (a ‑> a 而 A ‑> a)

3. 一个字符填充
'''''''''''''''''''''''''''

在可视化块模式可以使用 "r" 命令。输入一个字符，那么这个块就被这个字符填充了

4. 字符平移
'''''''''''''''''''''''''''

">" 命令把 **选中** 的文档向右移动一个 "平移单位"，中间用空白填充。平移的起始点是可
视列块的左边界(就是可视化块的最左边的那一条)。每次平移后要是想继续平移的话可以使用"."命令重复上一次的命令

平移的距离由 'shiftwidth' 选项定义。例如，要每次平移 4 个空格，可以用这个命
令:

::

    :set shiftwidth=4

"<" 命令向左移动一个 "平移单位"，但能移动的距离是有限的，因为它左边的不是空白
字符的字符会挡住它，这时它移到尽头就不再移动。

个人测试"<"命令好像无法使用
    
5. 连接若干行
'''''''''''''''''''''''''''

就是把多行变成了一行，然后原来的每行在一行之后就用一个空格来分隔

"J" 命令连接被选中的行。也就是删除所有的换行符。其实不只是换行符，行前后的多余
空白字符会一起被删除而全部用一个空格取代。如果行尾刚好是句尾，就插入两个空格
(参见 'joinspaces' 选项)
    
"J" 命令其实不关心选中了哪些字符，只关心块涉及到哪些行。所以可视列块的效果与
"v" 和 "V" 的效果是完全一样的。

如果你不想改变那些空白字符，可以使用 "gJ" 命令。 (不知道为什么，我这里"J"和"gJ"都一样的不删除空格)



9. 恢复
=================================

1. 基本恢复
-------------

::

    vim ‑r {file}

如果你编辑的时候没有给定文件名，可以用一个空的字符串来表示文件名:

::

    vim ‑r ""

    你需要进入原来的目录执行这个命令，否则 Vim 是找不到这个交换文件的

2. 交换文件在哪
--------------------------------------------

Vim 会列出所有它能找到的交换文件。它还会从其它目录寻找本目录文件的交换文件，但
它不会寻找其它目录里的交换文件，更不会遍及整个目录树

::

    vim ‑r

2.1 使用指定的交换文件
'''''''''''''''''''''''''''''''''

如果你知道要用哪个文件，你可以指定交换文件的名字。Vim 会找出交换文件所对应的原
始文件的名字。

::

    Vim ‑r file.swo

    这个方法在交换文件在一个非预期的目录中时很有用。Vim 知道 *.s[uvw][a‑z] 模式的
    文件是交换文件。




10. 快速键入命令行命令
============================================

1. 命令行编辑
---------------------------------

1. 移 动
'''''''''''''''''''''''''''''''''''''

命令行上移动光标时最常用的键:

::

    <Left>                      左移一个字符
    <Right>                     右移一个字符
    <S‑Left> 或 <C‑Left>        左移一个单词
    <S‑Right> 或 <C‑Right>      右移一个单词
    CTRL‑B 或 <Home>            命令行行首
    CTRL‑E 或 <End>             命令行行尾

2. 删 除
'''''''''''''''''''''''''''''''''''''

<BS>    键删除光标前一个字符
CTRL‑W  删除光标前整个单词。
CTRL‑U  删除命令行上全部文字，从而让你从头开始。

3. 替 换
''''''''''''''''''''''''''''''''''''''

插入键 <Insert> 让你在插入字符和替换字符两种方式之间切换。

在命令模式切换到替换字符的方式后，光标在其他字符上输入的话会替换掉当前的字符，且光标会向后移动

4. 取 消
'''''''''''''''''''''''''''''''''''''''''''''''''

你本想执行一个 : 或 / 命令，但却改变了主意。要清除命令行上你已经键入的文字却不 执行该命令，按 CTRL‑C 或 <Esc>。

.. note::

    <Esc> 是普遍采用的 "退出" 键。不幸的是，在过去美好的 Vi 版本里，在命令
    行上按 <Esc> 却是执行命令！由于那会被认为是程序 bug，Vim 采用 <Esc> 来
    取消命令。但其 'cpoptions' 选项可以使 Vim 跟 Vi 兼容。而且，使用映射时
    (那可能是为 Vi 而写的)，<Esc> 键也总和 Vi 兼容。由此看来，采用 CTRL‑C
    倒不失为一种永远奏效的方法。

2. 命令行缩写
---------------------------------

有些 ":" 命令确实很长。我们已经提及替代命令 ":substitute" 可以被缩写成 ":s"。
这是一个基本机理，即所有 ":" 冒号命令都可以被缩写。

一个一个地记


3. 命令行补全
-----------------

:<Tab>: 制表键 <Tab> 会补全光标前的词
:Ctrl+P: 反方向循环匹配列表

4. 查看匹配总览
---------------------------------

当你输入了一些命令，想补全的时候。

::

:CTRL‑D: 显示全部可以补全的命令

5. 显示历史命令
------------------

命令历史记录:

::

    :history

    "q"键退出查看命令历史

搜索历史记录:

::

    :history /


6. 命令行窗口
--------------

键盘上直接按"q:",进入命令行窗口

::

    q:

Vim 现在就在屏幕底部打开了一个 (很小的) 窗口。它存放着命令行历史记录

可以像平常一样对那些命令进行更改，光标所在行按<Enter>执行命令,按 <Enter> 键执行光标下的那个命令。这跟 Vim 处于插入模式抑或普通模式无关。

11. 离开和回来
=================

1. 挂起和继续
-------------

::suspend: 后台挂起
:CTRL‑Z: 后台挂起
:fg: shell环境下将后台的vim唤起


当你按 CTRL‑Z 行不通时，你还可以用命令 ":suspend"。别忘了把 Vim 引导回前
台，否则你会失去你所有的修改！

2. 执行shell命令
-----------------

从 Vim 内部执行单个外壳命令，用 ":!{command}"。

::

    :!{command}

    :!{program}         执行 {program}
    :r !{program}       执行 {program} 并读取其输出
    :w !{program}       执行 {program} 传送文本至其输入
    :[range]!{program}  经由 {program} 过滤文本



3. 记忆有关信息；viminfo
--------------------------

信息文件 viminfo 设计用来储存状态信息:

- 命令行和模式搜索的历史记录
- 寄存器内文本
- 各种文件的标记
- 缓存器列表
- 全局变量

4. 重返VIM中断处
-------------------

:'0: 每次退出 Vim，它都创建一个标记。最后那个是 '0。

来那个 '0 所指的位置就
成了 '1。而原来那个 '1 就成了 '2，依此类推。而原来的标记 '9 就没了。
要发现标记 '0 至 '9 指向何处， :marks 命令很有用。

5. 重返某文件
---------------

如果你想回到最近曾经编辑过的文件:

::

    :oldfiles

假如你要编辑第二个文件，也就是列表里 "2:" 开始的那个，输入:

::

    :e #<2

    :e      :edit   编辑
    #<2     oldfiles 命令里编号为2的文件，#号就是这样的，如果是%号，就是在本文件上修改，%<2,也就是本文件名加上2, 
            {file}2,变成了新建文件了

也可以这样来分割窗口来编辑第三个文件:

::

    :split #<3

简单点的方法：

::

    :browse oldfiles

假如你想编辑oldfiles的第2个命令，先按 "q" 停止列表，然后会有提示:

::

    Type number and <Enter> (empty cancels):

输入 "2" 并按 <Enter> 来编辑第二个文件。

6. 保存会话记录:
-----------------

::

    :mksession ~/.vim/mine.vim

    现在你可以用这个布局启动 Vim:

    vim ‑S ~/.vim/mine.vim

12. 寻找要编辑的文件
=================================================

1. 文件浏览器
---------------

进入当前目录,CWD

::

    :edit .

1. 按下 <Enter> 键。你就可以编辑那个文件了

2. 要回到浏览器，再用一次 ":edit ." 或":Explore" 即可

3. 浏览文件文件时，进入一个目录后，除了"../"回到上一级之外，可以使用"-"号来实现


<F1>键盘映射

::

          <F1>   导致 Netrw 发出帮助
          <cr>   Netrw 将进入目录或读取文件
          <del>  Netrw 将尝试删除文件/目录 
          <c-h>  编辑文件隐藏列表
          <c-l>  使 Netrw 刷新目录列表
          <c-r>  使用 gvim 服务器浏览
          <c-tab> 缩小/展开网络/探索窗口
            -    使 Netrw 上升一个目录
            a    在正常显示之间循环
                 隐藏（抑制显示匹配 g:netrw_list_hide 的文件）
                 和显示（只显示符合 g:netrw_list_hide 的文件）
            cd   将浏览目录设为当前目录
            C    设置编辑窗口
            d    创建目录
            D    尝试删除文件/目录
            gb   转到上一个书签目录
            gd   作为目录强制处理
            gf   强制处理为文件
            gh   快速隐藏/取消隐藏点文件
            gn   将树的顶部设置为光标下方的目录
            gp   更改仅限本地的文件权限
            i    控制列表风格 (瘦、长、宽和树形)。长列表包含文件大小和日期信息。
            I    切换横幅的显示方式
            mb   收藏当前目录
            mc   将标记文件复制到标记文件目标目录
            md   将差异应用于标记的文件（最多 3 个）
            me   将标记的文件放在参数列表中并对其进行编辑
            mf   标记文件
            mF   取消标记文件
            mg   将 vimgrep 应用于标记的文件
            mh   切换标记的文件足以出现在隐藏列表中
            mm   将标记的文件移动到标记的文件目标目录
            mp   打印标记文件
            mr   使用 shell 样式正则表达式标记文件
            mt   当前浏览目录成为标记文件目标
            mT   将 ctags 应用于标记的文件
            mu   取消标记所有标记的文件
            mv   对标记文件应用任意 vim 命令
            mx   将任意 shell 命令应用于标记的文件
            mX   将任意 shell 命令应用于整体标记的文件
            mz   压缩/解压缩标记文件
            o    在新的浏览器窗口中输入光标下的文件/目录。  使用水平分割。
            O    获取游标指定的文件
            p    预览文件
            P    在先前使用的窗口中浏览
            qb   列出已添加书签的目录和历史记录
            qf   显示文件信息
            qF   使用快速修复列表标记文件
            qL   使用位置列表标记文件
            r    逆转排列顺序。
            R    重命名指定文件/目录
            s    反复按 s 会改变文件排序的方式；可以按照名字、修改日期或文件大小排序。
            S    指定名称排序的后缀优先级
            t    在新标签页中输入光标下的文件/目录
            u    更改为最近访问过的目录
            U    更改为后续访问的目录
            v    在新的浏览器窗口中输入光标下的文件/目录。  使用垂直分割。
            x    查看带有关联程序的文件
            X    通过 system() 在光标下执行文件名
            %    在 netrw 的当前目录下打开一个新文件


netrw 浏览器不限于你的本地机器；也可使用 url，如下:
(拖尾的 / 是必需的)

::

    :Explore ftp://somehost/path/to/dir/
    :e scp://somehost/path/to/dir/

2. 当前目录
------------

1. ":cd" 命令可以用来改变当前目录(CWD)。

2. ":pwd" 命令来查看当前目录使什么

3. Vim 记得最近访问过的那个目录。你可以用 "cd ‑" 命令回去那儿。

4. 双窗口不同当前目录

::

    执行过 :lcd 命令的窗口记得它特有的当前目录。在其它窗口执行 :cd 或
    :lcd 命令对它毫无影响。

    在一个采用特有当前目录的窗口执行 :cd 命令以后，该窗口就回过头来又采用共享
    的当前目录了。

    也就是有两个窗口(:split),然后一个窗口使用了":lcd",切换到另外一个窗口上，对它使用":cd",然后这两个窗口就不同步一个当前目录(CWD)了，lcd,就是这样用的


5. 标签页本地目录

打开新的标签页时，使用打开时所在的之前标签页窗口的目录。可以用 :tcd 命令来改
变当前标签页的目录。除了有局部于窗口的目录的窗口以外，同一标签页的所有窗口共享
此目录。任何此标签页上打开的新窗口会使用此目录为当前工作目录。在标签页中使用
:cd 命令不会改变有局部于标签页目录的标签页的工作目录。在标签页中使用 :cd
命令改变全局目录后，也改变了当前标签页的工作目录。



3. 查找文件
----------------

假定现在你在编辑一个 C 程序，该程序有这样一行:

::

    #include "inits.h"

你想要查看文件 "inits.h" 里有些什么。把光标移到该文件名上，并键入:

::

    gf

    Vim 就会找到并打开这个文件。

如果该文件不在当前目录里怎么办？Vim 将利用 'path' 选项来寻找这个文
件。该选项是一系列目录名，Vim 会在其中为你寻找文件

假设你把你的头文件存放在目录 "/usr/include/" 里。下面这个命令把该目录加入
'path' 选项:

::

    绝对路径

    :set path+=/usr/include

    相对路径(用于当前目录下的):

    :set path+=./include

当你要找的文件名没出现在你编辑的文件里时，你可以键入这个文件名:

::

    :find inits.h

要在新窗口内打开那个已经找到的文件，用 CTRL‑W f，而不是 "gf"，或用 ":sfind" 而
不是 ":find"。

::

    CTRL‑W f

4. 缓冲区列表
----------------

Vim 编辑器使用术语 "缓冲区" 来描述编辑当中的文件。实际上，缓冲区是你编辑的文件
的副本。你修改完缓冲区，就把缓冲区的内容写进文件。缓冲区不仅存放文件内容，而且
还存放着全部标记，设定，以及其它跟被编辑文件相关的东西。

1. 隐藏缓冲区
'''''''''''''''''''''''''''''''''

当你编辑两个文件的时候，想切换到另外一个文件，但是又不能直接:edit 而且又不想直接存盘这个文件(有时候修改配置文件会实时生效),然后这样的话可以这样，隐藏掉缓冲区(还不如直接开个窗口):

::

    :hide edit two.txt

.. warning::

    小心！当你的隐藏的缓冲区已经改动，千万不要在所有缓冲区存盘之前就退出 Vim。

2. 非激活缓冲区
'''''''''''''''''''''''''''''''''

之前的都是激活的缓冲区,以及隐藏缓冲区。而非激活缓冲区，不输出文本，不显示在窗口内，它们更像是当你使用一些命令的时候，才显示的一些信息，比如之前的文本浏览历史记录什么的。

3. 缓冲区列表
'''''''''''''''''''''''''''''''''

可以用这个命令查看缓冲区列表:

::

    :buffers

    :ls

其输出可能像这样:

::

    1 %a + "_ccc/mydocs/note/source/Vim.rst" line 1303

第一栏存放缓冲区号。你可以利用它来编辑文件，而不用键入文件名，参见下文。
紧随缓冲区号的是些标志位。然后是文件名，以及光标最后一次停留的行号。
可能出现的标志位有以下这些 (自左至右):



:u:       列表外缓冲区 unlisted‑buffer 。
:%:       当前缓冲区。
:#:       轮换缓冲区。
:a:       激活缓冲区，缓冲区被加载且显示。
:h:       隐藏缓冲区，缓冲区被加载但不显示。
:=:       只读缓冲区。
:‑:       不可改缓冲区，'modifiable' 选项不置位。
:+:       已修改缓冲区。


4. 编辑缓冲区
'''''''''''''''''''''''''''''''''

你可以通过指定其缓冲区号来编辑一个缓冲区，而不必键入文件名:

::

    :buffer 2

但获知缓冲区号的唯一途径是查阅缓冲区列表。如果不用缓冲区号，你可以用文件名，或
其部分。Vim 将为你键入的文件名找到最佳匹配。如果只有一个缓冲区与之匹配，该缓冲区就被选
用。在这个例子中，被选中的就是 "help.txt"。

要在新窗口中打开一个缓冲区(这方法也适用于文件名。):

::

    :sbuffer 3

5. 使用缓冲区列表
'''''''''''''''''''''''''''''''''

你可以用这些命令在缓冲区列表间移动:

::bnext:          编辑下一个缓冲区
::bprevious:      编辑前一个缓冲区
::bfirst:         编辑第一个缓冲区
::blast:          编辑最后一个缓冲区

要从缓冲区列表上删除一个缓冲区，用这个命令:

::

    :bdelete 3

    同样，这命令也适用文件名。

如果你删除了一个激活的缓冲区 (显示在窗口中的缓冲区)，你也就关闭了该窗口。如
果你删除了当前缓冲区，你也就关闭了当前窗口。如果它是最后一个窗口，Vim 将找一个
缓冲区来编辑。你无法什么也不编辑！

.. note::

    即使用 ":bdelete" 命令删除了缓冲区以后，Vim 依然记得它。这个缓冲区实际
    上成了 "列表外" 缓冲区，它不再出现在 ":buffers" 命令所报告的列表中。不
    过 ":buffers!" 命令仍会列出 "列表外" 缓冲区 (没错，Vim 无所不能)。要让
    Vim 彻底忘记一个缓冲区，用 ":bwipe" 命令。另见 'buflisted' 选项。


13. 编辑特殊文件
=================================

1. DOS、Mac 和 Unix 文件
---------------------------------

当计算机问世以后，存储器曾经非常昂贵。有些人就认定没必要用两个字符来表示行
尾。

UNIX 开发者决定他们可以用 <New Line> 或 <NL> 一个字符来表示行尾

开发 Microsoft Windows 的那些家伙则决定沿用老式的 <CR><NL>

Apple 开发者规定了用 <CR>

unix  <NL>      在 Unix 上，<NL> 用于分行

dos   <CR><NL>

mac   <CR>

显示你当前使用的文件格式:

::

    :set fileformat?

下面这个命令告诉 Vim 先尝试用 UNIX 格式，其次，尝试
MS‑DOS 格式:

::

    :set fileformats=unix,dos


1. 强制格式
'''''''''''''''''''''''''''''''''

强制 Vim 忽略文件格式而使用你指定的格式:

::

    :edit ++ff=unix file.txt

    字符串 "++" 告诉 Vim 后面跟的是选项名，以取代其默认值。但仅作用于这一个命令。
    "++ff" 用于 'fileformat' 选项。你也可以用 "++ff=mac" 或 "++ff=dos"。
    这样用法并非适用于任意选项，目前 Vim 仅仅实现了 "++ff" 和 "++enc"。用全称
    "++fileformat" 和 "++encoding" 也行。

2. 转换
'''''''''''''''''''''''''''''''''

你可以用 'fileformat' 选项把文件从一种格式转换为另一种。

例如，假定你有个名为
README.TXT 的 MS‑DOS 文件，你要把它转换成 UNIX 格式。首先编辑这个采用 MS‑DOS
格式的文件:

::

    vim README.TXT

Vim 将识别出那是一个 dos 格式文件。现在把这个文件的格式改为 UNIX:

::

    :set fileformat=unix
    :write


2. 互联网上的文件
---------------------------------

当有链接显示时:

光标移到那个超链接里任何一个字符上，然后使用这个
命令:

::

    gf

    在另一个新窗口打开这个文件，则用 CTRL‑W f。

访问因特网上的文件要安装插件 netrw 才行。目前，下面这些格式的超文本链接可被识
别:

::

    ftp://     使用 ftp
    rcp://     使用 rcp
    scp://     使用 scp
    http://    使用 wget (只读)





3. 加密
---------------------------------

1. 加密文件
'''''''''''''''''''''''''''''''''


要开始编辑一个需要加密的新文件，可以用 "‑x" 参数来启动 Vim。例如:

::

    vim ‑x exam.txt

    小写x

当你用 Vim 编辑这个文件时，它就会要求你再输入那同一个密码。

2. 开/关加密
'''''''''''''''''''''''''''''''''

要给一个文件撤除加密，设定 'key' 选项为空字符串:

::

    :set key=

你下次把这个文件存盘时，存盘的文件就是未经加密的。

定 'key' 选项来启用加密是个坏主意。因为密码会被清清楚楚地显示在屏幕上。任
何人都可以偷看到你的密码。
为了避免这样的问题，创造了 ":X" 命令。它会像 "‑x" 参数向你索取一个密码:

::

    :X

    大写的X


4. 二进制文件
---------------------------------

1. 编辑二进制文件
''''''''''''''''''''''''''''''''''''''''''''

可以用 Vim 来编辑二进制文件。Vim 本非为此而设计的，因而有若干局限。但你能读
取一个文件，改动一个字符，然后把它存盘。结果是你的文件就只有那一个字符给改了，
其它的就跟原来那个一模一样。
要保证 Vim 别把它那些聪明的窍门用错地方，启动 Vim 时加上 "‑b" 参数:

::

    vim ‑b datafile

这个参数设定了 'binary' 选项。其作用是排除所有的意外副作用。例如，'textwidth'
设为零，免得文本行给擅自排版了。并且，文件一律以 Unix 文件格式读取。

二进制模式可以用来修改某程序的消息报文。小心别插入或删除任何字符，那会让程序运
行出问题。用 "R" 命令进入替换模式。

文件里的很多字符都是不可显示的。用 Hex 格式来显示它们的值:

::

    :set display=uhex

另外，也可以用命令 "ga" 来显示光标下的字符值。当光标位于一个 <Esc> 字符上时，
该命令的输出看起来就像这样:

::

    <^[> 27， Hex 1b, Octal 033


文件中也许没那么多换行符。你可以关闭 'wrap' 选项来获得总览的效果:

::

    :set nowrap

2. 字节位置
''''''''''''''

要发现你在文件中的当前字节位置，请用这个命令:

::

    g CTRL‑G

其输出十分冗长:

::

    Col 9‑16 of 9‑16; Line 277 of 330; Word 1806 of 2058; Byte 10580 of 12206

最后两个数字就是文件中的当前字节位置和文件字节总数。这已经考虑了 'fileformat'
选项导致换行符字节不同的影响。

要移到文件中某个指定的字节，请用 "go" 命令。例如，要移到字节 2345:

::

    2345go

3. 使用XXD
''''''''''''''

一个真正的二进制编辑器用两种方式来显示文本: 二进制和十六进制格式。你可以在 Vim
里通过转换程序 "xxd" 来达到这效果。该程序是随 Vim 一起发布的。

首先以二进制方式编辑这个文件:

::

    vim ‑b datafile

现在用 xxd 把这个文件转换成十六进制:

::

    :%!xxd

现在你可以随心所欲地阅读和编辑这些文本了。 Vim 把这些信息当作普通文本来对待。
修改了十六进制部分并不导致可显示字符部分的改变，反之亦然。
最后，用下面的命令把它转换回来:

::

    :%!xxd ‑r

只有十六进制部分的修改才会被采用。右边可显示文本部分的修改忽略不计。

欲知更多详情，参见 xxd 手册。




4. 压缩文件
---------------------------------


你可以像编辑任何其它文件一样，来编辑一个经过压缩的文件。插件 "gzip"
负责在你编辑这个文件时把它解压缩，以及在你存盘时再把它压缩起来。
目前支持的压缩方法有下面这些:

::

    .Z      compress
    .gz     gzip
    .bz2    bzip2

Vim 调用上面提到的程序来实际完成压缩和解压缩。你也许需要先安装这些程序。


5. 对齐文本
---------------

1. 行居中
''''''''''''''''''''''''''''''''''''''''''''

::

    :{range}center [width]

{range} 即是那些通常命令所能指定的范围。 [width] 是一个可选项，用来确定要居中
的行的宽度。如果没有指定 [width]，则默认使用 'textwidth' 的值。(如果
'textwidth' 是 0，则默认为 80。)

2. 右对齐
''''''''''''''''''''''''''''''''''

":right" 命令将使文本右对齐:

::

    :{range}right [width]

    这里的[width]表示的是距离最右边多少格

3. 左对齐
'''''''''''''''''''''''''''''''''

":left" 命令将文本左对齐

::

    :{range}left [width]

    这里的[width]表示的是距离最左边多少格


















14. Tap区和buffer区
==================================

:let mapleader = " ": 设置leader键为空格
:nnoremap tr :Lex<CR>:vertical resize 30<CR>: 打开文件树，选择文件编辑时用

1. buffer 缓冲区
----------------------

:nnoremap <leader>b :enew<CR>: 新建一个buffer
:nnoremap <leader>bn :bnext<CR>: 切换到下一个buffer
:nnoremap <leader>bd :bdelete<CR>: 删除这个buffer
:map <leader>ba :bufdo bd<cr>: 关闭所有buffer

2. tab
------------------------------------

:map <leader>tn :tabnew<cr>:  新建一个tab
:map <leader>tm :tabmove: 移动这个tab
:nnoremap <leader>l :tabnext<CR>: 切换到下一个tab
:nnoremap <leader>h :tabprevious<CR>: 切换到上一个tab 
:map <leader>te :tabedit <C-r>=escape(expand("%:p:h"), " ")<cr>/: 打开带有当前路径的新选项卡
:map <leader>tc :tabclose<cr>: 关闭这个tab
:map <leader>to :tabonly<cr>: 删除其他tab只留当前这个tab



::

    let g:lasttab = 1
    nmap <leader>tl :exe "tabn ".g:lasttab<CR>
    au TabLeave * let g:lasttab = tabpagenr()

    <leader>tl 切换到上一个标签,两个标签反复横跳




15. 有用的技巧
=====================================================

1.可视模式下按*或#搜索当前选择
--------------------------------------------

::

    vnoremap <silent> * :<C-u>call VisualSelection('', '')<CR>/<C-R>=@/<CR><CR>
    vnoremap <silent> # :<C-u>call VisualSelection('', '')<CR>?<C-R>=@/<CR><CR>

2. 按下<leader>键和<回车键>停止高亮显示搜索
------------------------------------------------------------------

::

    map <silent> <leader><cr> :noh<cr>

3. 打开文件时返回到最后编辑位置(您想要这个!)
------------------------------------------------------------------

::

    au BufReadPost * if line("'\"") > 1 && line("'\"") <= line("$") | exe "normal! g'\"" | endif

4. 启动/关闭粘贴模式
------------------------------------------------------------------

::

    map <leader>pp :setlocal paste!<cr>

5. 查阅 man 信息
------------------------------------------------------------------

man 手册中查询某个命令或者函 数的用法先用一个简单的方法: 把鼠标移到对应的单词 上然后输入:

::

    K

这种方法的缺点是你不能同时查看手册和编辑文档。这里有一种办法可以把手册显示到一 个 Vim 的窗口中。首先，加载 man 文件类型的外挂:

::

    :runtime! ftplugin/man.vim

如果你经常用到这种方法，可以把这个命令加到你的 vimrc 文件中。现在你可以用
":Man" 命令打开一个显示 man 手册的窗口了:

::

    :Man vim

你可以在这个新的窗口中上下滚动，而手册的本文会用语法高亮的形式显示。这样，你可 以找到需要的地方，并用 CTRL‑W w 跳转到原来的窗口中继续工作。 要指定手册的章节，可以在手册名称前面指定。例如，要找第三章的 "waybar":

::

    :Man 5 waybar
    
要跳转到另一个由 "word(1)" 形式定义的手册，只要在上面敲 CTRL‑]。无论怎样，
":Man" 命令总使用同一个窗口。

要显示当前光标下的单词的手册，这样:

::

    \K

    (如果你重定义了 <Leader>，用那个字符代替上面命令的反斜杠)。
    对我来说，也就是空格键

6. 不退出文件，刷新文件
------------------------

::

    :so %

16. Vim 插件
==============

1. 下载Vundle
-------------

::

    git clone https://github.com/VundleVim/Vundle.vim.git ~/.vim/bundle/Vundle.vim

2. 配置.vimrc,使其使用Vundle
-----------------------------

::


    set nocompatible              " be iMproved, required
    filetype off                  " required

    " set the runtime path to include Vundle and initialize
    set rtp+=~/.vim/bundle/Vundle.vim
    call vundle#begin()
    " alternatively, pass a path where Vundle should install plugins
    "call vundle#begin('~/some/path/here')

    " let Vundle manage Vundle, required
    Plugin 'VundleVim/Vundle.vim'
    "Plugin 'tpope/vim-fugitive' " plugin on GitHub repo 
    "Plugin 'airblade/vim-gitgutter'
    Plugin 'preservim/nerdcommenter'
    Plugin 'preservim/nerdtree'
    Plugin 'tpope/vim-surround'
    Plugin 'jiangmiao/auto-pairs'
    "Plugin 'scrooloose/syntastic'
    "Plugin 'valloric/youcompleteme'
    "Plugin 'vimwiki/vimwiki'
    "Plugin 'frazrepo/vim-rainbow'
    "Plugin 'itchyny/lightline.vim'
    "Plugin 'flazz/vim-colorschemes'
    Plugin 'preservim/tagbar'
    " different version somewhere else.
    " Plugin 'ascenator/L9', {'name': 'newL9'}

    " All of your Plugins must be added before the following line
    "
    call vundle#end()            " required
    filetype plugin indent on    " required

启动 vim 并运行 :PluginInstall

配置了：

:<leader>f: 打开NERDtree
:<leader>g: 打开tagbar
:<leader>cc: 快捷行注释
:<leader>ci: 取消行注释
:<leader>cy: 快捷行注释,再加上复制原内容
